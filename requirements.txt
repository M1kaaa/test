# Зависимости для веб-API (FastAPI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
openpyxl>=3.1.0

# Для GUI (tkinter обычно входит в стандартную установку Python)
# Если нужен более продвинутый GUI, можно использовать:
# PyQt5>=5.15.0
# или
# PySide6>=6.5.0
