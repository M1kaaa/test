# Калькулятор длины патч-кордов для дата-центра

Программа для расчёта необходимой длины патч-кордов при коммутации серверов в дата-центре.

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

**Windows:**
- Просто дважды кликните на файл **`запустить.bat`**
- Всё запустится автоматически: API, веб-сервер, браузер откроется сам

**Linux/Mac:**
```bash
python run_app.py
```

### Вариант 2: Ручной запуск

Если нужно запустить компоненты отдельно:

1. **Запустить API:**
```bash
python web_api.py
```

2. **Открыть веб-интерфейс:**
- Просто откройте `web_interface.html` в браузере
- Или используйте `run_app.py` для автоматического запуска

## 📋 Описание

Программа рассчитывает длину патч-корда на основе:
- Номера стоек и юнитов двух серверов (только стойки 02b03–02b18 из Excel)
- Физических параметров дата-центра (высота юнита, расстояние между стойками и т.д.)
- Правил прокладки кабеля (вертикальные и горизонтальные компоненты)

## 🎨 Визуализация

Веб-интерфейс показывает:
- **Общую длину кабеля** (с запасом и округлением)
- **Детализацию по сегментам:**
  - Вертикаль от сервера A до кабель-канала
  - Горизонталь по кабель-каналу между стойками
  - Вертикаль от сервера B до кабель-канала
- **Визуальную полосу** с пропорциональным отображением каждого сегмента

## 📐 Алгоритм расчёта

### Одна стойка
```
Длина = |unit_a - unit_b| × unit_height
```

### Разные стойки
```
Длина = vertical_a + vertical_b + horizontal + overhead

где:
- vertical_a = (max_unit - unit_a) × unit_height + margin_to_tray
- vertical_b = (max_unit - unit_b) × unit_height + margin_to_tray
- horizontal = |rack_a - rack_b| × distance_between_racks
- overhead = +1 м (если стойки соседние)
```

### Финальная длина
```
Длина_с_запасом = Длина × slack_factor
Итоговая_длина = округление_вверх(Длина_с_запасом, rounding_step)
```

## ⚙️ Конфигурация

Все параметры можно настроить через `DataCenterCableConfig`:

```python
config = DataCenterCableConfig(
    unit_height_m=0.044,                    # Высота 1U в метрах
    max_unit=50,                             # Максимальный юнит
    vertical_margin_to_cable_tray_m=0.20,   # Отступ до кабель-канала
    distance_between_adjacent_racks_m=0.60, # Расстояние между стойками
    additional_overhead_for_adjacent_racks_m=1.0,  # Доп. метраж для соседних
    slack_factor=1.10,                      # Коэффициент запаса
    rounding_step_m=0.5                     # Шаг округления
)
```

## 📝 Примеры использования

### Python код
```python
from cable_calculator import (
    ServerLocation,
    DataCenterCableConfig,
    calculate_patch_cord_length_m
)

# Одна стойка
server_a = ServerLocation(rack=1, unit=10)
server_b = ServerLocation(rack=1, unit=30)
length = calculate_patch_cord_length_m(server_a, server_b)
print(f"Длина: {length:.2f} м")

# Разные стойки
server_c = ServerLocation(rack=1, unit=5)
server_d = ServerLocation(rack=5, unit=45)
length2 = calculate_patch_cord_length_m(server_c, server_d)
print(f"Длина: {length2:.2f} м")
```

## 🔧 Установка зависимостей

```bash
pip install -r requirements.txt
```

## 📁 Структура проекта

- `cable_calculator.py` - основной модуль расчёта
- `rack_plan.py` - загрузка плана стоек из Excel (02b03–02b18)
- `web_api.py` - REST API для расчётов
- `web_interface.html` - веб-интерфейс с визуализацией
- `run_app.py` - автоматический запуск всего приложения
- `запустить.bat` - батник для Windows (двойной клик)

## 📄 Лицензия

Проект создан для внутреннего использования в дата-центре.
